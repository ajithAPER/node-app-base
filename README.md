# Basic Express, React, ApolloClient and graphql setup.

### Start up steps.
* Clone project
* Run ``npm install``.
* Run ``npm run dev`` for development.
* Run ``npm run build`` to build production ready code. Code gets transpiled into ``./build/`` directory.
